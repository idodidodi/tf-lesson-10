.DS_Store

# Local .terraform directories (The node_modules of Terraform)
**/.terraform/*

# .tfstate files (Contains sensitive data and environment state)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (Likely to contain sensitive secrets)
# Exception: You might want to allow 'example.tfvars'
*.tfvars
*.tfvars.json

# Ignore override files (Used for local tweaks)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc